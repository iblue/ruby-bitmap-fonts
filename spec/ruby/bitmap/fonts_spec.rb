require "spec_helper"

RSpec.describe Ruby::Bitmap::Fonts do
  it "has a version number" do
    expect(Ruby::Bitmap::Fonts::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
